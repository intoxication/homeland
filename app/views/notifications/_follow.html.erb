<% user = notification.actor %>
<% if user.blank? %>
 Related information has been deleted
<% else %>
  <div class="media-heading">
    <%= user_name_tag(user) %> Began to pay attention to you.
  </div>
<% end %>
